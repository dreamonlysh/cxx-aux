# cxx-aux

#### Description

Commonly used lite libraries and c++ standard library extensions for guyuemeng.

####

- estd

the `estd` is short for extension of cpp standard which provide some definition and structure

- binary

the `binary` is used to decode elf
